using Godot;
using System;

//Want to use members of Casters like the method GetGlobalMousePosition and the property Position (i.e. instance of Node2D probs enough
//But should one require that Casters have implementations of these things, or just assume that anything that implements the interface is a Node2D instance, or is there a nicer solutioin?

public interface ICaster
{
	
}
